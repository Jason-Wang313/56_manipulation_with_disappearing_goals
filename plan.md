# Plan

1. Preserve the v2 re-binding ambiguity stress as a negative control, not as the final paper decision.
2. Build a RAM-light deterministic full-scale suite over task family, horizon, occlusion, ambiguity, observability, cost, and policy factors.
3. Stream compact condition rows and generated tables/figures to `results/full_scale/` without loading the full condition CSV into memory.
4. Rewrite the anonymous ICLR manuscript around the final claim: persistent proxies are useful only with explicit identity re-binding, re-acquisition, ambiguity reporting, and cost accounting.
5. Expand the paper to at least 25 pages with full-scale methods, stress analysis, implementation patterns, claim guardrails, and limitations.
6. Compile the PDF with the final build script, require at least 25 pages, export only to `C:/Users/wangz/Downloads/56.pdf`, and remove `paper/main.pdf`.
7. Render the Downloads PDF to PNG and visually inspect representative pages before commit.
8. Commit and push only after docs, validation metadata, stale-status scans, JSON checks, LaTeX checks, file-size checks, and cleanup pass.
